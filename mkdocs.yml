site_name: triwarp
site_description: Triangular mesh library based on NVIDIA Warp
site_url: https://lucagrementieri.github.io/triwarp/
repo_url: https://github.com/lucagrementieri/triwarp
docs_dir: docs

# `benchmarks/plot_comparison.py` writes a `.md` table alongside every chart PNG under
# `assets/benchmarks/`, for the "full results appendix" case (rendering every cell, not just the
# curated few `docs/benchmarks.md` embeds by hand) -- those sidecar tables are data, not nav pages,
# so they're excluded here rather than left to surface as unlinked stray pages.
exclude_docs: |
  assets/benchmarks/*.md

theme:
  name: material
  logo: assets/logo.svg
  favicon: assets/logo.svg
  features:
    - navigation.tabs
    - navigation.sections
    - navigation.indexes
    - navigation.top
    - toc.follow
    - content.code.copy
    - search.suggest
  palette:
    - media: "(prefers-color-scheme: light)"
      scheme: default
      primary: custom
      accent: custom
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      primary: custom
      accent: custom
      toggle:
        icon: material/brightness-4
        name: Switch to light mode

extra_css:
  - stylesheets/extra.css

markdown_extensions:
  - admonition
  - tables
  - pymdownx.details
  - pymdownx.superfences

plugins:
  - search
  - gen-files:
      scripts:
        - docs/gen_ref_pages.py
  - literate-nav:
      nav_file: SUMMARY.md
  - section-index
  - mkdocstrings:
      handlers:
        python:
          inventories:
            - https://docs.python.org/3/objects.inv
            - https://numpy.org/doc/stable/objects.inv
            - https://docs.scipy.org/doc/scipy/objects.inv
            - https://trimesh.org/objects.inv
          options:
            docstring_style: numpy
            docstring_section_style: table
            show_source: true
            show_root_heading: true
            merge_init_into_class: true
            separate_signature: true
            show_signature_annotations: true
            show_if_no_docstring: true
            members_order: source
