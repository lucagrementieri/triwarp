.benchmarks/
promesh/
downloads/
plans/

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/
.ruff_cache/

# Coverage artifacts. `.coverage` is the local data file and `coverage.xml` is what the CI job
# hands to Codecov; both are written into the repo root by a `pytest --cov` run.
.coverage
.coverage.*
coverage.json
coverage.xml
htmlcov/

# Virtual environments
.venv

# Docs build output
site/
docs/api/
docs/SUMMARY.md

# Mesh files
*.obj
*.off
*.ply
*.stl
tests/data/
benchmarks/data/
# Reference libraries (development only, see .claude/CLAUDE.md §6 and §9)
reference/
